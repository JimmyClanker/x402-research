services:
  - type: web
    runtime: node
    name: x402-research
    plan: free
    buildCommand: npm ci --omit=dev
    startCommand: node server.js
    envVars:
      - key: PORT
        value: 4021
      - key: NODE_ENV
        value: production
